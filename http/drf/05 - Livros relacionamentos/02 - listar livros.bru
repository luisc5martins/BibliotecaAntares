meta {
  name: 02 - Listar livros (200)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/livros/
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.results: isDefined
}

docs {
GET /api/livros/ — leitura usa serializer com depth=1 (seção 11):
categoria, editora e autores vêm como objetos aninhados.
Nunca asserir contagem fixa.
}